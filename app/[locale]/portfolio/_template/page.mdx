import Link from 'next/link'
import { getDictionary } from '../../../i18n/getDictionary'
import PCBViewer from '../../../components/PCBViewer'
import { motion } from 'framer-motion'
import { Cpu, Zap, Shield, Globe, Wrench, CheckCircle2, Settings, Box, CircuitBoard, Layers } from 'lucide-react'

export async function generateMetadata({ params }) {
  return {
    title: 'Project Title — Subtitle',
    description: 'Short project description for SEO.'
  }
}

export default async function Page({ params }) {
  const locale = params?.locale || 'en'
  const t = await getDictionary(locale)

  return (
    <article className="max-w-4xl">
      {/* Breadcrumb */}
      <motion.div 
        className="mb-6"
        initial={{ opacity: 0, y: 10 }}
        animate={{ opacity: 1, y: 0 }}
      >
        <Link 
          href={`/${locale}/portfolio`} 
          className="text-sm text-neutral-600 dark:text-neutral-400 hover:text-neutral-900 dark:hover:text-white transition-colors"
        >
          ← {t.common.backToPortfolio}
        </Link>
      </motion.div>
      
      {/* Header */}
      <motion.div
        initial={{ opacity: 0, y: 20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.5 }}
      >
        <div className="flex items-center gap-3 mb-4">
          <div className="p-3 rounded-xl bg-gradient-to-br from-blue-100 to-cyan-100 dark:from-blue-900/20 dark:to-cyan-900/20">
            <Cpu className="w-8 h-8 text-[#0077b6]" />
          </div>
          <h1 className="text-4xl md:text-5xl font-bold text-neutral-900 dark:text-white">
            Project Title — Subtitle
          </h1>
        </div>
        <p className="text-xl text-neutral-600 dark:text-neutral-300 mt-4">
          Short project description (one sentence).
        </p>
      </motion.div>

      {/* 3D PCB Viewer */}
      <motion.div 
        className="mt-8"
        initial={{ opacity: 0, y: 20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ delay: 0.2 }}
      >
        <PCBViewer stepFile={null} />
      </motion.div>

      {/* Overview */}
      <motion.section 
        className="mt-12"
        initial={{ opacity: 0, y: 20 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true }}
      >
        <div className="p-6 rounded-2xl border border-neutral-200 dark:border-neutral-800 bg-white dark:bg-neutral-950">
          <div className="flex items-start gap-3">
            <div className="p-2 rounded-lg bg-neutral-100 dark:bg-neutral-800 shrink-0">
              <Globe className="w-5 h-5 text-neutral-900 dark:text-white" />
            </div>
            <div>
              <h2 className="text-xl font-bold text-neutral-900 dark:text-white mb-2">Overview</h2>
              <p className="text-neutral-600 dark:text-neutral-300">
                Brief overview of project (1-2 paragraphs). What is it? What problem does it solve?
              </p>
            </div>
          </div>
        </div>
      </motion.section>

      {/* Design Philosophy */}
      <motion.section 
        className="mt-8"
        initial={{ opacity: 0, y: 20 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true }}
        transition={{ delay: 0.1 }}
      >
        <div className="p-6 rounded-2xl border border-neutral-200 dark:border-neutral-800 bg-white dark:bg-neutral-950">
          <div className="flex items-start gap-3">
            <div className="p-2 rounded-lg bg-neutral-100 dark:bg-neutral-800 shrink-0">
              <Zap className="w-5 h-5 text-neutral-900 dark:text-white" />
            </div>
            <div>
              <h2 className="text-xl font-bold text-neutral-900 dark:text-white mb-2">Design Philosophy</h2>
              <p className="text-neutral-600 dark:text-neutral-300">
                What principles guided design decisions? What makes this solution unique?
              </p>
            </div>
          </div>
        </div>
      </motion.section>

      {/* Key Features */}
      <motion.section 
        className="mt-12"
        initial={{ opacity: 0, y: 20 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true }}
      >
        <h2 className="text-2xl font-bold text-neutral-900 dark:text-white mb-6">Key Features</h2>
        <div className="grid md:grid-cols-2 gap-4">
          {[
            { icon: CheckCircle2, title: "Feature 1", description: "Description of feature" },
            { icon: Shield, title: "Feature 2", description: "Description of feature" },
            { icon: Settings, title: "Feature 3", description: "Description of feature" },
            { icon: Box, title: "Feature 4", description: "Description of feature" },
          ].map((feature, index) => (
            <motion.div
              key={index}
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ delay: index * 0.1 }}
            >
              <div className="p-6 rounded-2xl border border-neutral-200 dark:border-neutral-800 bg-white dark:bg-neutral-950 hover:border-neutral-300 dark:hover:border-neutral-700 transition-colors">
                <div className="flex items-start gap-3">
                  <div className="p-2 rounded-lg bg-neutral-100 dark:bg-neutral-800 shrink-0">
                    <feature.icon className="w-5 h-5 text-neutral-900 dark:text-white" />
                  </div>
                  <div>
                    <h3 className="font-semibold text-neutral-900 dark:text-white mb-1">
                      {feature.title}
                    </h3>
                    <p className="text-sm text-neutral-600 dark:text-neutral-300">
                      {feature.description}
                    </p>
                  </div>
                </div>
              </div>
            </motion.div>
          ))}
        </div>
      </motion.section>

      {/* Technical Implementation */}
      <motion.section 
        className="mt-12"
        initial={{ opacity: 0, y: 20 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true }}
      >
        <h2 className="text-2xl font-bold text-neutral-900 dark:text-white mb-6">Technical Implementation</h2>
        
        <div className="space-y-4">
          {[
            {
              icon: CircuitBoard,
              title: "Subsystem 1",
              description: "Description of first technical subsystem.",
              specs: [
                { label: "Detail 1", value: "Technical specification" },
                { label: "Detail 2", value: "Technical specification" },
                { label: "Detail 3", value: "Technical specification" },
                { label: "Detail 4", value: "Technical specification" }
              ]
            },
            {
              icon: Layers,
              title: "Subsystem 2",
              description: "Description of second technical subsystem.",
              specs: [
                { label: "Detail 1", value: "Technical specification" },
                { label: "Detail 2", value: "Technical specification" },
                { label: "Detail 3", value: "Technical specification" },
                { label: "Detail 4", value: "Technical specification" }
              ]
            }
          ].map((subsystem, index) => (
            <motion.div
              key={index}
              initial={{ opacity: 0, x: -20 }}
              whileInView={{ opacity: 1, x: 0 }}
              viewport={{ once: true }}
              transition={{ delay: index * 0.1 }}
              className="flex gap-4"
            >
              <div className="shrink-0 w-12 h-12 rounded-xl bg-gradient-to-br from-[#0077b6] to-[#00b4d8] flex items-center justify-center text-white">
                <subsystem.icon className="w-6 h-6" />
              </div>
              <div className="flex-1 pb-6 border-b border-neutral-200 dark:border-neutral-800 last:border-0 last:pb-0">
                <h3 className="font-semibold text-neutral-900 dark:text-white mb-1">
                  {subsystem.title}
                </h3>
                <p className="text-neutral-600 dark:text-neutral-300 mb-3">
                  {subsystem.description}
                </p>
                <div className="grid grid-cols-2 gap-2 text-sm">
                  {subsystem.specs.map((spec, i) => (
                    <div key={i} className="flex justify-between">
                      <span className="text-neutral-500 dark:text-neutral-400">{spec.label}:</span>
                      <span className="text-neutral-900 dark:text-white font-medium">{spec.value}</span>
                    </div>
                  ))}
                </div>
              </div>
            </motion.div>
          ))}
        </div>
      </motion.section>

      {/* Performance Specifications */}
      <motion.section 
        className="mt-12"
        initial={{ opacity: 0, y: 20 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true }}
      >
        <div className="p-6 rounded-2xl border border-neutral-200 dark:border-neutral-800 bg-white dark:bg-neutral-950">
          <div className="flex items-start gap-3 mb-4">
            <div className="p-2 rounded-lg bg-neutral-100 dark:bg-neutral-800 shrink-0">
              <Wrench className="w-5 h-5 text-neutral-900 dark:text-white" />
            </div>
            <h2 className="text-xl font-bold text-neutral-900 dark:text-white">Performance Specifications</h2>
          </div>
          <div className="grid md:grid-cols-2 gap-3 text-sm">
            {[
              { label: "Spec 1", value: "Value" },
              { label: "Spec 2", value: "Value" },
              { label: "Spec 3", value: "Value" },
              { label: "Spec 4", value: "Value" },
              { label: "Spec 5", value: "Value" },
              { label: "Spec 6", value: "Value" },
            ].map((spec, index) => (
              <div key={index} className="flex justify-between py-2 border-b border-neutral-100 dark:border-neutral-800 last:border-0">
                <span className="text-neutral-500 dark:text-neutral-400">{spec.label}:</span>
                <span className="text-neutral-900 dark:text-white font-medium">{spec.value}</span>
              </div>
            ))}
          </div>
        </div>
      </motion.section>

      {/* Applications */}
      <motion.section 
        className="mt-8"
        initial={{ opacity: 0, y: 20 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true }}
      >
        <div className="p-6 rounded-2xl border border-neutral-200 dark:border-neutral-800 bg-white dark:bg-neutral-950">
          <div className="flex items-start gap-3 mb-4">
            <div className="p-2 rounded-lg bg-neutral-100 dark:bg-neutral-800 shrink-0">
              <Globe className="w-5 h-5 text-neutral-900 dark:text-white" />
            </div>
            <h2 className="text-xl font-bold text-neutral-900 dark:text-white">Applications</h2>
          </div>
          <p className="text-neutral-600 dark:text-neutral-300 mb-4">
            What applications is this project suitable for?
          </p>
          <div className="flex flex-wrap gap-2">
            {["Application 1", "Application 2", "Application 3", "Application 4"].map((app, index) => (
              <span
                key={index}
                className="px-3 py-1.5 rounded-lg border border-neutral-200 dark:border-neutral-800 bg-neutral-50 dark:bg-neutral-900 text-sm text-neutral-700 dark:text-neutral-300"
              >
                {app}
              </span>
            ))}
          </div>
        </div>
      </motion.section>

      {/* Testing & Validation */}
      <motion.section 
        className="mt-8"
        initial={{ opacity: 0, y: 20 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true }}
      >
        <div className="p-6 rounded-2xl border border-neutral-200 dark:border-neutral-800 bg-white dark:bg-neutral-950">
          <div className="flex items-start gap-3 mb-4">
            <div className="p-2 rounded-lg bg-neutral-100 dark:bg-neutral-800 shrink-0">
              <CheckCircle2 className="w-5 h-5 text-neutral-900 dark:text-white" />
            </div>
            <h2 className="text-xl font-bold text-neutral-900 dark:text-white">Testing & Validation</h2>
          </div>
          <p className="text-neutral-600 dark:text-neutral-300 mb-4">
            What testing was performed to validate the design?
          </p>
          <div className="space-y-2 text-sm">
            {[
              "Test 1: Description of test and results",
              "Test 2: Description of test and results",
              "Test 3: Description of test and results",
              "Test 4: Description of test and results",
              "Test 5: Description of test and results"
            ].map((test, index) => (
              <div key={index} className="flex items-start gap-2">
                <CheckCircle2 className="w-4 h-4 text-green-600 dark:text-green-400 shrink-0 mt-0.5" />
                <span className="text-neutral-700 dark:text-neutral-300">{test}</span>
              </div>
            ))}
          </div>
        </div>
      </motion.section>

      {/* Closing */}
      <motion.section 
        className="mt-8"
        initial={{ opacity: 0, y: 20 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true }}
      >
        <div className="p-6 rounded-2xl bg-gradient-to-br from-neutral-100 to-neutral-50 dark:from-neutral-900 dark:to-neutral-950 border border-neutral-200 dark:border-neutral-800">
          <p className="text-neutral-700 dark:text-neutral-300 text-center">
            Closing statement summarizing the project and its achievements.
          </p>
        </div>
      </motion.section>
    </article>
  )
}
